<Window x:Class="A3sist.UI.UI.ChatWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="A3sist Chat" Height="600" Width="500"
        WindowStartupLocation="CenterScreen"
        Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#FF2D2D30" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="A3sist Chat" Foreground="White" FontWeight="Bold" 
                              FontSize="14" VerticalAlignment="Center"/>
                    <Ellipse Width="8" Height="8" x:Name="ConnectionIndicator" 
                            Fill="Red" Margin="10,0,5,0" VerticalAlignment="Center"/>
                    <TextBlock x:Name="ConnectionStatus" Text="Disconnected" 
                              Foreground="White" FontSize="10" VerticalAlignment="Center"/>
                </StackPanel>
                
                <Button Grid.Column="1" x:Name="ClearChatButton" Content="Clear" 
                       Background="Transparent" Foreground="White" BorderThickness="1" 
                       BorderBrush="White" Padding="8,4" Margin="0,0,5,0"
                       Click="ClearChatButton_Click"/>
                
                <Button Grid.Column="2" x:Name="SettingsButton" Content="⚙" 
                       Background="Transparent" Foreground="White" BorderThickness="0" 
                       FontSize="16" Width="30" Click="SettingsButton_Click"/>
            </Grid>
        </Border>
        
        <!-- Chat Messages -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" 
                     x:Name="ChatScrollViewer" Padding="10">
            <ItemsControl x:Name="ChatMessages" Background="White">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,5,0,5" Padding="10" CornerRadius="8" MaxWidth="400">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Role}" Value="User">
                                            <Setter Property="Background" Value="#FF2196F3"/>
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Role}" Value="Assistant">
                                            <Setter Property="Background" Value="#FFF5F5F5"/>
                                            <Setter Property="HorizontalAlignment" Value="Left"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel>
                                <TextBlock TextWrapping="Wrap" FontSize="13" LineHeight="18">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Role}" Value="User">
                                                    <Setter Property="Foreground" Value="White"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Role}" Value="Assistant">
                                                    <Setter Property="Foreground" Value="Black"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                    <Run Text="{Binding Content}"/>
                                </TextBlock>
                                <TextBlock FontSize="10" Margin="0,5,0,0" HorizontalAlignment="Right">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Role}" Value="User">
                                                    <Setter Property="Foreground" Value="#FFBBBBBB"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Role}" Value="Assistant">
                                                    <Setter Property="Foreground" Value="Gray"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                    <Run Text="{Binding Timestamp, StringFormat='HH:mm:ss'}"/>
                                    <Run Text=" • "/>
                                    <Run Text="{Binding ModelUsed}"/>
                                </TextBlock>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        
        <!-- Model Selection -->
        <Border Grid.Row="2" Background="#FFF9F9F9" Padding="10,5">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Model:" VerticalAlignment="Center" Margin="0,0,8,0" FontWeight="Bold"/>
                <ComboBox x:Name="ModelComboBox" Width="200" DisplayMemberPath="Name" 
                         SelectedValuePath="Id" SelectionChanged="ModelComboBox_SelectionChanged"/>
                <Button x:Name="RefreshModelsButton" Content="🔄" Width="30" Height="25" 
                       Margin="8,0,0,0" Click="RefreshModelsButton_Click" ToolTip="Refresh Models"/>
                <TextBlock x:Name="ModelStatusText" VerticalAlignment="Center" Margin="10,0,0,0" 
                          FontSize="10" Foreground="Gray"/>
            </StackPanel>
        </Border>
        
        <!-- Input Area -->
        <Grid Grid.Row="3" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Text Input -->
            <Border Grid.Row="0" BorderBrush="#FFCCCCCC" BorderThickness="1" CornerRadius="4">
                <TextBox x:Name="ChatInput" AcceptsReturn="True" TextWrapping="Wrap" 
                        MaxHeight="100" VerticalScrollBarVisibility="Auto" BorderThickness="0"
                        Padding="8" FontSize="13" KeyDown="ChatInput_KeyDown"/>
            </Border>
            
            <!-- Send Controls -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" 
                       Margin="0,8,0,0">
                <TextBlock Text="Ctrl+Enter to send" FontSize="10" Foreground="Gray" 
                          VerticalAlignment="Center" Margin="0,0,10,0"/>
                <Button x:Name="SendButton" Content="Send Message" Padding="15,5" 
                       Click="SendButton_Click" IsEnabled="False">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#FF2196F3"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Style.Triggers>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="#FFCCCCCC"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</Window>