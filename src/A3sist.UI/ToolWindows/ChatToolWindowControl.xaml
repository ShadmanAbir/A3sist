<UserControl x:Class="A3sist.UI.ToolWindows.ChatToolWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vs="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="400"
             Background="{DynamicResource VsBrush.Window}">
    
    <Grid>
        <!-- Main content area where the chat interface will be loaded -->
        <ContentControl x:Name="MainContent" 
                       HorizontalContentAlignment="Stretch"
                       VerticalContentAlignment="Stretch"/>
        
        <!-- Loading indicator (initially visible until chat interface is ready) -->
        <Grid x:Name="LoadingGrid" 
              Background="{DynamicResource VsBrush.Window}"
              Visibility="Visible">
            <StackPanel HorizontalAlignment="Center" 
                       VerticalAlignment="Center">
                <TextBlock Text="🤖" 
                          FontSize="48"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,16"/>
                <TextBlock Text="Initializing A3sist Chat..." 
                          Foreground="{DynamicResource VsBrush.WindowText}"
                          HorizontalAlignment="Center"
                          FontSize="14"/>
                <ProgressBar IsIndeterminate="True"
                           Width="200"
                           Height="4"
                           Margin="0,16,0,0"
                           Style="{DynamicResource VsProgressBarStyle}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>